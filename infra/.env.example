# Compose-level overrides. Defaults in docker-compose.yml are dev-safe.
# Copy to infra/.env if you want to change defaults locally.

POSTGRES_DB=product_catalog
POSTGRES_USER=app
POSTGRES_PASSWORD=app

# Generate with: docker compose run --rm backend php artisan key:generate --show
APP_KEY=

# Shared secret used by Laravel to call Next.js /api/revalidate.
# Set the same value in frontend/.env.local as REVALIDATE_SECRET.
REVALIDATE_SECRET=dev-only-secret
